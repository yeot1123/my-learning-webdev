


const samplearray = [];

samplearray.push(1);
samplearray.push(2, 3, 4, 5);

console.log(`Array list: ${samplearray}`);

samplearray.pop();
console.log(`Array list pop(): ${samplearray}`);

//Array on JS stack
samplearray.unshift(5); // InsertHead
console.log(`Array list pop(): ${samplearray}`);
